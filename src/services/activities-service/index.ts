async function validateVacancieActivityByTime(userId: number, activity: number) {
  console.log(userId, activity);
}

const activitiesService = {
  validateVacancieActivityByTime,
};

export default activitiesService;
